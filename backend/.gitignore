target/
*.class
.idea/
*.iml

# Generated by the Docker build (frontend dist copied in as static resources) — never hand-edit
# or commit this, it's produced fresh on every build.
src/main/resources/static/

# Real local secrets — never commit this. Copy application-local.yml.example instead.
src/main/resources/application-local.yml
